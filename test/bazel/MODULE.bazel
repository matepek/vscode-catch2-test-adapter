bazel_dep(name = "rules_cc", version = "0.2.20")

# https://registry.bazel.build/modules/googletest
# should be in sync with test/cpp/gtest/GoogleTest.cmake !!!
bazel_dep(name = "googletest", version = "1.17.0")
